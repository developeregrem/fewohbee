{{ form_start(form) }}
    {{ form_row(form.accountNumber) }}
    {{ form_row(form.name) }}
    {{ form_row(form.type) }}
    {{ form_row(form.isCashAccount) }}
    {{ form_row(form.sortOrder) }}
    <div class="d-grid">
        <button type="submit" class="btn btn-primary">
            <i class="fas fa-save me-1"></i> {{ 'button.save'|trans }}
        </button>
    </div>
{{ form_end(form) }}
