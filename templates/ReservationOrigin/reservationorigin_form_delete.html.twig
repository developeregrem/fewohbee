<div class="row">
    <form id="origin-form-{{ id }}" class="form-horizontal" role="form" onsubmit="return doDeleteOrigin({{ id }})">
        <div class="col-md-11">
            <p>{{ 'reservationorigin.delete.ask'|trans }}</p>
        </div>
        <div class="col-md-11">
            <input type="submit" value="{{ 'button.delete'|trans }}" class="btn btn-danger"/>
            <button class="btn btn-default"
                    onclick="return collapseOrigin({{ id }})">{{ 'button.cancel'|trans }}</button>
        </div>
        <input name="_csrf_token" value="{{ token }}" type="hidden">
    </form>
</div>

