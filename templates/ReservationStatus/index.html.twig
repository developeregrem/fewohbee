{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }} -  {{ 'status.title'|trans }}
{% endblock %}

{% block description %}
    {{ parent() }} -  {{ 'status.description'|trans }}
{% endblock %}

{% block content %}
<div class="container" data-controller="settings">
    <div class="row">
        <div class="col">
            <button type="button" class="btn btn-secondary"
                    data-action="click->settings#openModalAction"
                    data-url="{{ path('reservation_status_new') }}"
                    data-title="{{ 'status.add'|trans }}"
                    data-bs-toggle="modal"
                    data-bs-target="#modalCenter">
                <i class="fas fa-plus"></i> {{ 'status.add'|trans }}</button>
        </div>
    </div>
    <div class="row mt-3">
        <div class="col">
            <table class="table table-bordered table-hover">
                <thead>
                <tr>
                    <th>{{ 'status.name'|trans }}</th>
                    <th>{{ 'status.action'|trans }}</th>
                </tr>
                </thead>
                <tbody>
                {% for status in reservation_status %}
                    <tr>
                        <td>
                            <div class="reservation-inner" style="hight:100%; background-color: {{ status.color}}; color: {{ status.contrastColor }}">{{ status.name }}</div>
                        </td>
                        <td>
                            <a href="#" title="{{ 'button.details'|trans }}" data-bs-toggle="modal" data-bs-target="#modalCenter">
                                <i class="fas fa-info-circle"
                                   data-action="click->settings#openModalAction"
                                   data-url="{{ path('reservation_status_edit', {'id': status.id}) }}"
                                   data-title="{{ 'button.details'|trans }}"></i></a>
                            <a href="#" title="{{ 'button.edit'|trans }}" data-bs-toggle="modal" data-bs-target="#modalCenter">
                                <i class="fas fa-edit"
                                   data-action="click->settings#openModalAction"
                                   data-url="{{ path('reservation_status_edit', {'id': status.id}) }}"
                                   data-title="{{ 'button.edit'|trans }}"
                                   data-enable-edit="true"></i></a>
                            {% set id = status.id %}
                            {% set targetUrl = path('reservation_status_delete', {'id': status.id}) %}
                            {% use "common/delete_popover.html.twig" %}
                            <a href="#" title="{{ 'button.delete'|trans }}">
                                <i class="fas fa-trash-alt"
                                   data-popover="delete"
                                   data-title="{{ 'registrationbook.delete.ask'|trans }}"
                                   data-bs-content='{{ block('deletePopoverContent') }}'></i></a>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>
{% endblock %}
