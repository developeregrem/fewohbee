<div class="modal-body">
    <div class="pl-5 pr-5">
        <div class="row">
                <div class="col" id="flash-message-overlay">
                </div>
        </div>
        <div class="row">
            <div class="col">
                <input type="text" class="form-control" id="lastname" name="lastname"
                       placeholder="{{ 'reservation.select.customer.search.input.placeholder'|trans }}">
            </div>
            <div class="col text-right">
                <button class="btn btn-default" type="button" onclick="getCustomers(1)">
                    <i class="fas fa-search"></i> {{ 'nav.customer.search'|trans }}
                </button>
                <button class="btn btn-default" type="button" onclick="getFormForNewCustomer()">
                    <i class="fas fa-plus"></i> {{ 'nav.customer.add'|trans }}
                </button>
            </div>
        </div>
        <div class="row mt-4">   
            <div class="col">
            <form id="customer-selection" onsubmit="return createNewCustomer()">
                <div id="customers">
                    <p>
                        {% if customer %}
                            Bereits selektiert: {{ customer.firstname }} {{ customer.lastname }}
                        {% endif %}
                    </p>
                </div>
                <input type="submit" value="{{ 'button.save'|trans }}" class="btn btn-primary float-right d-none"/>
            </form>
            </div>
        </div>
    </div>
</div>
<div id="createNewCustomerFooter" class="modal-footer">
    <ul class="pagination mr-auto">
        <li class="page-item " onclick="return selectAppartment(false)">
            <a class="page-link" href="#">&larr; {{ 'button.back'|trans }}</a></li>
    </ul>
    {% if customer %}
    <ul class="pagination">
        <li class="page-item" onclick="return getReservationPreview({{ customer.id }})">
            <a class="page-link" href="#">{{ 'button.continue.with'|trans }} {{ customer.firstname }} {{ customer.lastname }}&rarr;</a>
        </li>
    </ul>
    {% endif %}
</div>
<script type="text/javascript">
    // enable keyup for search
    $("#lastname").delayKeyup(function () {
        getCustomers(1);
    }, 400);
</script>